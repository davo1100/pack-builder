services:
  - type: web
    name: pack-builder
    runtime: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python studio.py
    autoDeployTrigger: commit
    healthCheckPath: /studio.html
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.8"
