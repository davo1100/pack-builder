services:
  - type: web
    name: pack-builder
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python studio.py
    autoDeploy: true
    healthCheckPath: /studio.html
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.8"
